fastapi
uvicorn[standard]
python-dotenv
pydantic-settings

# --- LlamaIndex and Google AI Dependencies ---
# Pin google-generativeai to the range required by the specific LlamaIndex adapters below
google-generativeai>=0.5.2,<0.6.0 # Both adapters 0.2.0/0.3.1 need this range

# Use llms-gemini 0.3.1 (requires core < 0.12.0)
llama-index-llms-gemini==0.3.1

# Use embeddings-google 0.2.0 (requires core < 0.12.0 and >= 0.11.0)
llama-index-embeddings-google==0.2.0

# Pin core LlamaIndex to match the adapter requirements (<0.12.0 and >=0.11.0)
llama-index>=0.11.0,<0.12.0
llama-index-core>=0.11.0,<0.12.0

# Let pip choose compatible versions for these based on the core version
llama-index-readers-file
llama-index-vector-stores-chroma

# --- Other Dependencies ---
chromadb>=0.4.0

# --- Testing Dependencies ---
pytest
httpx