# Example Environment Variables for Codex AI Backend
# ---------------------------------------------------
# Copy this file to .env and replace the placeholder values with your actual credentials.
# DO NOT commit the .env file to version control.

# --- Google AI Credentials ---
# Required for accessing Gemini LLM and Embedding models via Google Generative AI API.
# Obtain your key from Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# --- Application Settings (Optional examples, adjust as needed) ---

# Set the host and port for the FastAPI backend server (if overriding defaults)
# BACKEND_HOST=0.0.0.0
# BACKEND_PORT=8000

# Set the base URL for the React frontend server (used for CORS configuration)
# Useful if your frontend runs on a non-standard port or domain during development
# FRONTEND_URL=http://localhost:3000

# Enable debug mode (e.g., for more verbose logging or specific dev features)
# Set to "False" in production
# DEBUG_MODE=True

# --- Database Settings (Placeholder examples, only needed if using a persistent DB beyond ChromaDB's file storage) ---

# Example for PostgreSQL
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Example for SQLite (if storing metadata)
# SQLITE_DB_PATH=/path/to/your/metadata.db

# --- Vector Store Settings (ChromaDB specific, if needed) ---

# Path where ChromaDB should persist its data locally (if not using defaults or client/server mode)
# CHROMA_PERSIST_DIRECTORY=/path/to/chroma_db_persistence

# --- Secret Key (Optional example, needed for features like session management or security) ---
# Generate a strong random key (e.g., using `openssl rand -hex 32`)
# SECRET_KEY=YOUR_STRONG_RANDOM_SECRET_KEY_HERE

# --- Logging Configuration (Optional example) ---
# LOG_LEVEL=INFO # Example: DEBUG, INFO, WARNING, ERROR, CRITICAL